# AIDE File Integrity Check Configuration
# Builds on common.conf foundation

# AIDE-specific directories (builds from common REPORT_DIR)
DB_ARCHIVE="/var/lib/aide/archive"

# Email configuration for AIDE alerts
AIDE_EMAIL_FROM="aide+${SERVER_NAME}@${EMAIL_DOMAIN}"